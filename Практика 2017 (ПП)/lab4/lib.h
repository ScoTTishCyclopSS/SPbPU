#pragma ones
char* delete_str(char str[], int n1, int n2);//удаляет из строки символы в диапазоне; Принимает строку и диапазон значений; Выводит либо ошибку, либо изменённую строку
char* add_str(char str[], char substr[], int n1, int n2);//Добавляет дополнительную строку; Принимает строку и диапазон значений; Выводит либо ошибку, либо изменённую строку
